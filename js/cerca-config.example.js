// Runtime configuration template for the contact form.
//
// For LOCAL development: copy this file to js/cerca-config.js (which is
// gitignored) and set your address below. The real js/cerca-config.js is
// never committed to source control.
//
// In PRODUCTION: GitHub Actions generates js/cerca-config.js at deploy time
// from the CONTACT_EMAIL repository secret (see .github/workflows/deploy.yml).
window.CERCA_CONTACT_EMAIL = 'you@example.com';
